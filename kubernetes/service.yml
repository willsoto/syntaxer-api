apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: syntaxer-api
    tier: backend
spec:
  type: LoadBalancer
  ports:
  - port: 8080
  selector:
    app: syntaxer-api
    tier: backend
